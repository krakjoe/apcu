// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("apcue", "for apcue support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("apcue", "enable apcue support", "no");

if (PHP_APCUE != "no") {
	EXTENSION("apcue", "apcue.c");
}

